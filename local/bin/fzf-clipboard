#! /usr/bin/env bash

if [[ -z "$PICKER_OPENED" ]]; then
  export PICKER_OPENED=1
  xdg-terminal-exec --app-id='term.floating' "$0" "$@"
fi

cliphist list |
  fzf --style=minimal --preview="cliphist decode {1}" |
  cliphist decode |
  wl-copy
