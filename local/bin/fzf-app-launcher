#! /usr/bin/env bash

app=$(
  fd -H -e desktop . /usr/share/applications ~/.local/share/applications -0 |
    xargs -0 -n1 basename |
    sed 's/\.[^.]*$//' |
    fzf
)
[[ $app ]] || exit
setsid uwsm app $app.desktop &
sleep 0
